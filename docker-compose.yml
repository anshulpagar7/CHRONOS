# One-command demo: docker compose up --build
# Dashboard at http://localhost:8080, Prometheus metrics at /metrics.
services:
  chronos:
    build: .
    command: ["--demo", "--demo-rate=3", "--workers=3"]
    ports:
      - "8080:8080"
    restart: unless-stopped
